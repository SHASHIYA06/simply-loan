import React from 'react';
import { Card, CardContent } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { CheckCircle } from 'lucide-react';
import { Link } from 'react-router-dom';
import { createPageUrl } from '@/utils';

export default function Step4_Success({ application }) {
  return (
    <Card className="border-0 shadow-lg bg-white/80 backdrop-blur-sm">
      <CardContent className="p-8 text-center">
        <CheckCircle className="w-24 h-24 text-green-500 mx-auto mb-6" />
        <h2 className="text-3xl font-bold text-slate-900 mb-4">Application Submitted!</h2>
        <p className="text-slate-600 mb-2">
          Thank you, {application.personal?.name}. Your application has been received.
        </p>
        <p className="text-slate-600 mb-6">
          Your application ID is: <strong className="text-blue-600">{application.application_id}</strong>
        </p>
        <p className="text-slate-600 mb-8">
          Our team will review your application and get in touch with you shortly with the best offers.
        </p>
        <div className="flex justify-center gap-4">
          <Link to={createPageUrl("LandingPage")}>
            <Button variant="outline">Back to Home</Button>
          </Link>
          <Link to={createPageUrl("AdminDashboard")}>
            <Button>Track My Application</Button>
          </Link>
        </div>
      </CardContent>
    </Card>
  );
}
